package com.a3bradesco.chat.chat;

// Enum que define os tipos de mensagens possíveis no chat

public enum MessageType {

    CHAT, // Mensagem normal
    JOIN, // Quando um usuário entra
    LEAVE // Quando um usuário sai
}
